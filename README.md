# Url Shortener Code Test

## Getting Started

Clone the repo:
`git clone git@github.com:lukemarsh/url-shortener-code-test.git`

Install dependencies:
`npm install`

Run the application:
`npm start`

Open the browser at: http://localhost:3000

## Running tests

`npm test`

## Running the linter

`npm run lint`